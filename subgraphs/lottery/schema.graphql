# TicketEvent
type Ticket @entity {
  id: ID! # transaction
  who: Bytes! #address
  ticketSize: BigInt!
  timestamp: BigInt!
}

type Reward @entity {
  id: ID! # transation
  to: Bytes! # address
  amount: BigInt!
  collateral: Bytes!
  nft: Bytes!
  nftid: BigInt!
  timestamp: BigInt!
}