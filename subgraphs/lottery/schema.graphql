# TicketEvent
type Ticket @entity {
  id: ID! # transaction
  who: Bytes! #address
  ticketSize: BigInt!
  timestamp: BigInt!
}

type Reward @entity {
  id: ID! # transation
  to: Bytes! # address
  amount: BigInt!
  timestamp: BigInt!
}